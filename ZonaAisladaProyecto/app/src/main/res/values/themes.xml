<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZonaAisladaProyecto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ZonaAisladaProyecto" parent="Base.Theme.ZonaAisladaProyecto" />

    <style name="Theme.ZonaAisladaProyecto.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ZonaAisladaProyecto.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ZonaAisladaProyecto.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>